{
  "name": "gv-api-web",
  "version": "2.1.272",
  "scripts": {
    "build": "npm run clean && npm run generate-api",
    "generate-api": "node bash/generate",
    "clean": "node bash/clean"
  },
  "browser": {
    "fs": false
  },
  "dependencies": {
    "querystring": "^0.2.0",
    "isomorphic-unfetch": "3.0.0"
  },
  "devDependencies": {
    "shelljs": "^0.8.3",
    "dotenv": "^8.0.0"
  }
}
